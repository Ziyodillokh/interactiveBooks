# Fail `pnpm install` fast on a wrong runtime (the machine default is Node 20). Note that the
# pnpm 11 shim itself aborts under Node < 22.13 before this file is read — see DEVELOPMENT.md.
engine-strict=true
